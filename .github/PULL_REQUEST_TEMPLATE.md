## What

<!-- Short summary of the change -->

## Why

<!-- The problem this solves -->

## Checklist

- [ ] Tests added/updated (`go test ./...` passes)
- [ ] `gofmt -l .` is empty and `go vet ./...` is clean
- [ ] Docs updated if the CLI surface changed
- [ ] Conventional-commit PR title (e.g. `feat:`, `fix:`, `docs:`)
